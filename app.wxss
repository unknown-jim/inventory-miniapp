page {
  /* 旧档位。2026-08-26 起 docs/ui-scale.md 已废止「正文 36rpx、可点 96rpx」这一套，
     这里保留为别名过渡，新代码一律不要再用；等 pages 全部迁完由收尾批统一删除。
     --chip-min 已并入 --tap-min（88rpx），0b 批起全仓零消费，同样保留待删。

     --fs-hero 是唯一还带着旧值的名字：文档定 80rpx，这里仍是 48rpx。0b 批刻意不动它，
     因为它的 5 个消费点语义互不相同，翻值会一次改错三处：
       · pages/index/index.wxss 的 .hero-title 与 .hero-profit-num 才是文档说的看板 hero，
         但改到 80rpx 之前必须先落地文档要求的按位数降档 class，否则 549 万那种
         13 字符金额在首屏排不下。
       · pages/customer-edit/customer-edit.wxss 的 .debt-num 是客户欠款，
         文档 2026-08-30 已把它迁到 --fs-hero-md（68rpx），不是 --fs-hero。
       · pages/products 与 pages/product-detail 的 .thumb-text 是商品图占位文字，
         根本不属于 hero 档，48rpx 提到 80rpx 会把两列图卡里的占位字撑爆。
     交给各自的逐屏批处理。tests/record-sheet.test.js 有断言钉着 48rpx。 */
  --fs-xs: 32rpx;
  --fs-sm: 34rpx;
  --fs-md: 36rpx;
  --fs-lg: 40rpx;
  --fs-xl: 44rpx;
  --fs-hero: 48rpx;
  --tap-sm: 96rpx;
  --chip-min: 80rpx;

  /* docs/ui-scale.md（2026-08-26 改版）的完整档位表，值与文档表格逐格对齐。
     0b 批把 --tap-md 与 --tap-lg 这两个名字从旧值翻到文档值：
       · --tap-md  104rpx -> 96rpx （输入框、次级按钮、可点列表行）
       · --tap-lg  112rpx -> 104rpx（主按钮、底部固定 CTA）
     两处按设计稿把数值钉死的消费点已同批改绑，视觉零变化：
       · pages/index 的记一笔主入口改用 --tap-lg，仍是 104rpx = 稿 4:983 的 52px。
       · pages/records 的 FAB 改写字面量 112rpx，稿 4:985 是 56×56，
         新档位表里没有这一档，不该再借 --tap-lg 的数值。 */
  --fs-caption: 24rpx;
  --fs-label: 26rpx;
  --fs-body: 28rpx;
  --fs-title: 32rpx;
  --fs-amount: 32rpx;
  --fs-amount-lg-sm: 32rpx;
  --fs-amount-lg: 40rpx;
  --fs-display: 48rpx;
  --fs-hero-sm: 56rpx;
  --fs-hero-md: 68rpx;
  --tap-min: 88rpx;
  --tap-md: 96rpx;
  --tap-lg: 104rpx;

  /* 颜色取自设计稿 Semantic 层（ui-scale.md 只管字号热区密度，没有颜色表）。
     0a 批起，app.wxss 的共用类全部消费这些变量，不再写死颜色。
     主行动一律黑是 docs/design-file.md 记的历轮刻意决策，不要回退成 brand 青绿。 */
  --color-fill-action: #171717;
  --color-fill-action-pressed: #404040;
  --color-text-primary: #171717;
  --color-text-muted: rgba(23, 23, 23, 0.62);
  --color-text-faint: rgba(23, 23, 23, 0.5);
  --color-text-on-action: #FFFFFF;
  --color-bg-card: #FFFFFF;
  --color-bg-overlay: rgba(15, 20, 26, 0.45);
  --color-border-default: rgba(23, 23, 23, 0.1);
  --color-neutral-100: #F5F5F5;
  --color-neutral-200: #E5E5E5;
  --color-neutral-600: #525252;
  --color-text-debt: #DB2626;
  /* 0a 批新增。bg/page 3:72 -> neutral/50 3:14；fill/ghost 3:91 -> neutral/100 3:15；
     border/brand 3:87 -> neutral/900 3:23；neutral/500 3:19 是禁用实底档的字色。 */
  --color-bg-page: #FAFAFA;
  --color-fill-ghost: #F5F5F5;
  --color-border-brand: #171717;
  --color-neutral-500: #737373;
  /* 5a 批新增。neutral/300 3:17 是 chip 铁律第五档「＋添加」的描边色
     （docs/ui-scale.md：白底 + neutral/300 描边 = chip/add）。稿上的本体是
     组件库 Row/Chip 的 13:703，Screen/04 与 Screen/16 共用同一枚，所以它归 app.wxss，
     不留在页面里。 */
  --color-neutral-300: #D4D4D4;
  /* 1a 批新增。全局阻断态的图标底与字形色，取自稿 Row/00「全局阻断态（4 种）」4:1099：
     维护 4:1100 与搬家中 4:1104 绑 Primitive amber/50 3:29 与 amber/700 3:33
     （Semantic 别名 fill/warning-subtle 3:96 与 text/warning 3:84）；
     版本过旧 4:1107 与未建店 4:1110 绑 Primitive slate/100 3:65 与 slate/700 3:68。
     slate 那一对的 Semantic 别名叫 tag/adjust-bg 3:111 与 tag/adjust-fg 3:110，语义是
     库存调整标签色，**不要复用那两个名字**，复用会把两条语义焊死。
     对比度：#B5540A 压 #FFFAEB = 4.79:1；#334054 压 #F2F5FA = 9.66:1，都过 4.5:1。 */
  --color-amber-50: #FFFAEB;
  --color-amber-700: #B5540A;
  --color-slate-100: #F2F5FA;
  --color-slate-700: #334054;
  /* 2a 批新增。取自稿 Row/01：
     amber/600 3:32 是欠款横幅 banner/debt 3:402 的图标与 chevron 描边色
     （B1 已加的 amber/50 3:29 是它的底、amber/700 3:33 是它的文字）；
     red/600 3:37 是要补货行「剩 X」的红 —— 与已有的 --color-text-debt 同值，
     但 design-file.md 记着红的三种语义（欠款金额 / 危险动作 / 库存预警）是三条，
     借用会在任何一条改色时一次改错两处，所以按 Primitive 名单列；
     green/700 3:83 是今日毛利数字（Semantic 别名 text/success，但落点是毛利不是「成功」，
     不复用那个名字）。
     对比度：#DB2626 压白 4.86:1；#D97805 压 #FFFAEB 3.03:1（非文字，过 3:1）；
     #057857 压白 5.48:1。 */
  --color-amber-600: #D97805;
  --color-red-600: #DB2626;
  --color-green-700: #057857;
  /* 1a 批新增。动效 token 取自稿「Section/动效与层级」13:90 的 b1MotionTokens（13:60），
     原话是「实现用这些名，不要在页面里另写毫秒数」。
     层级 token（sticky 10 · fab 100 · overlay 1000 · sheet 1001 · dialog 1100）本批不加：
     现有弹层写的是字面量，改它们属于弹层批，本批一个消费点都没有。 */
  --duration-fast: 120ms;
  --duration-base: 200ms;
  --duration-slow: 320ms;
  --easing-standard: cubic-bezier(0.2, 0, 0, 1);
  --easing-enter: cubic-bezier(0, 0, 0, 1);
  --easing-exit: cubic-bezier(0.3, 0, 1, 1);
  --space-xs: 8rpx;
  --space-sm: 12rpx;
  --space-md: 16rpx;
  --space-lg: 24rpx;
  --space-xl: 32rpx;
  --page-pad: var(--space-lg);
  --card-pad: 28rpx;
  --gap: var(--space-md);
  min-height: 100%;
  background: var(--color-bg-page);
  color: var(--color-text-primary);
  font-size: var(--fs-body);
  box-sizing: border-box;
}

button {
  padding: 0;
}

button::after {
  border: none;
}

.page {
  padding: var(--page-pad) var(--page-pad) calc(var(--page-pad) * 2);
  font-size: var(--fs-body);
}

.card {
  background: var(--color-bg-card);
  border-radius: 24rpx;
  padding: var(--card-pad);
  box-shadow: 0 8rpx 24rpx rgba(15, 20, 26, 0.04);
}

.section-title {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin: 36rpx 8rpx var(--space-md);
  font-size: var(--fs-title);
  font-weight: 600;
}

.muted {
  color: var(--color-text-muted);
  font-size: var(--fs-label);
}

.row {
  display: flex;
  align-items: center;
}

.space {
  justify-content: space-between;
}

.btn-primary {
  height: var(--tap-lg);
  line-height: var(--tap-lg);
  background: var(--color-fill-action);
  color: var(--color-text-on-action);
  border-radius: 16rpx;
  font-size: var(--fs-title);
  font-weight: 600;
}

.btn-primary[disabled] {
  background: var(--color-fill-ghost);
  color: var(--color-neutral-500);
  opacity: 1;
}

.btn-ghost {
  height: var(--tap-md);
  line-height: var(--tap-md);
  background: var(--color-fill-ghost);
  color: var(--color-text-primary);
  border-radius: 14rpx;
  font-size: var(--fs-body);
}

.btn-danger {
  height: var(--tap-md);
  line-height: var(--tap-md);
  background: #FEF2F2;
  color: #DC2626;
  border-radius: 14rpx;
  font-size: var(--fs-body);
}

.tag {
  display: inline-block;
  padding: var(--space-xs) var(--space-md);
  border-radius: 8rpx;
  font-size: var(--fs-caption);
}

.tag-alert {
  background: #FEF2F2;
  color: #DC2626;
}

.tag-ok {
  background: #ECFDF5;
  color: #047857;
}

.tag-in {
  background: #ECFEFF;
  color: #0E7490;
}

.tag-out {
  background: #FFF7ED;
  color: #C2410C;
}

.tag-pay {
  background: #F5F3FF;
  color: #6D28D9;
}

.tag-credit {
  background: #FEF2F2;
  color: #DC2626;
}

.tag-spec {
  background: #ECFEFF;
  color: #0E7490;
}

.tag-blank {
  background: #EEF2FF;
  color: #4338CA;
}

.tag-return {
  background: #FFF7ED;
  color: #C2410C;
}

.tag-convert {
  background: #F5F3FF;
  color: #6D28D9;
}

.tag-adjust {
  background: #F1F5F9;
  color: #334155;
}

.chips {
  display: flex;
  flex-wrap: wrap;
  gap: var(--gap);
  margin-bottom: var(--space-md);
}

.field-input + .chips,
.axis-title + .chips {
  margin-top: var(--space-md);
}

.field-hint {
  margin-top: var(--space-md);
}

.field-hint + .chips {
  margin-top: var(--space-sm);
}

.chip {
  display: flex;
  align-items: center;
  min-height: var(--tap-min);
  padding: var(--space-sm) 28rpx;
  box-sizing: border-box;
  background: var(--color-fill-ghost);
  border: 2rpx solid transparent;
  border-radius: 999rpx;
  color: var(--color-neutral-600);
  font-size: var(--fs-body);
  white-space: normal;
  line-height: 1.3;
}

.chip.on {
  background: var(--color-bg-card);
  border-color: var(--color-border-brand);
  color: var(--color-text-primary);
  font-weight: 600;
}

/* chip 铁律的第五档：白底 + neutral/300 描边 = 「＋添加」（稿 chip/add 13:703）。
   它靠**形**分家（白底描边）而不是靠字色 —— 只差字色的话屏上和禁用档读不出差别，
   这条写在 docs/ui-scale.md 的铁律表里。字色用 neutral/500，与稿的 13:704 同值。 */
.chip.add {
  background: var(--color-bg-card);
  border-color: var(--color-neutral-300);
  color: var(--color-neutral-500);
}

.chip.low {
  background: #FEF2F2;
  color: #DC2626;
}

.field {
  margin-bottom: var(--space-lg);
}

.field-label {
  color: var(--color-text-muted);
  font-size: var(--fs-label);
  margin-bottom: var(--space-sm);
}

.field-input {
  height: var(--tap-md);
  padding: 0 var(--space-lg);
  background: var(--color-fill-ghost);
  border-radius: 14rpx;
  font-size: var(--fs-body);
}

.field-textarea {
  min-height: 160rpx;
  height: auto;
  padding: 20rpx var(--space-lg);
  line-height: 1.5;
  width: 100%;
  box-sizing: border-box;
}

.search {
  height: var(--tap-md);
  padding: 0 28rpx;
  background: var(--color-fill-ghost);
  border-radius: 40rpx;
  margin-bottom: 20rpx;
  font-size: var(--fs-body);
}

.empty {
  padding: 80rpx var(--space-lg);
  text-align: center;
  color: var(--color-text-muted);
}

.empty-title {
  font-size: var(--fs-title);
  font-weight: 600;
  color: var(--color-text-primary);
  margin-bottom: var(--space-sm);
}

/* 空态插画的槽位。稿 state/empty 3:408 的 icon 框是 48×48（= 96rpx）、居中、
   与标题之间 gap 8（= 16rpx）。**本类只给几何，不给图**：三枚插画
   （纸箱 10:36 / 票据 10:28 / 人形 10:32）是各屏自己的内容，规则已导出到
   inventory-miniapp-handoffs/spec-1a-assets/empty-icons.wxss，哪一屏要就把
   对应那一条粘进那一屏的 wxss，用法是 class="empty-icon empty-icon-products"。 */
.empty-icon {
  width: 96rpx;
  height: 96rpx;
  margin: 0 auto var(--space-md);
  background-repeat: no-repeat;
  background-position: center;
  background-size: 96rpx 96rpx;
}

/* 空态的引导操作行。稿 state/empty/cta 3:751 的 cta-row 3:756：横排、居中、
   按钮间 gap 12（= 24rpx = --space-lg），与上面副文案之间 gap 8（= 16rpx）。
   窄屏允许折行（docs/ui-scale.md 密度规则：并排操作允许折行，不缩小字号）。 */
.empty-actions {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: var(--space-lg);
  margin-top: var(--space-md);
}

.empty-actions > button {
  flex: 0 1 auto;
  margin: 0;
}

.page-loading {
  margin-top: var(--space-xl);
}

.stat-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--space-lg) var(--gap);
}

.stat {
  min-width: 0;
}

.stat-num {
  font-size: var(--fs-amount-lg);
  font-weight: 700;
}

.action-strip {
  display: flex;
  flex-wrap: wrap;
  gap: var(--gap);
  margin-top: var(--space-lg);
}

.action-strip > button {
  flex: 1 1 28%;
  min-width: 160rpx;
  margin: 0;
  box-sizing: border-box;
  white-space: normal;
  padding-left: var(--space-md);
  padding-right: var(--space-md);
}

.action-strip .btn-ghost,
.action-strip .btn-primary {
  height: auto;
  min-height: var(--tap-md);
  line-height: 1.3;
  padding-top: var(--space-sm);
  padding-bottom: var(--space-sm);
}

.action {
  min-height: var(--tap-md);
  height: auto;
  line-height: 1.3;
  padding: 20rpx var(--space-md);
  background: var(--color-bg-card);
  border: 2rpx solid var(--color-border-brand);
  color: var(--color-text-primary);
  border-radius: 16rpx;
  font-size: var(--fs-body);
  white-space: normal;
}

.field-row {
  display: flex;
  gap: var(--gap);
}

.half {
  flex: 1;
  min-width: 0;
}

.seg {
  display: flex;
  flex-wrap: wrap;
  gap: var(--gap);
  margin-bottom: var(--space-sm);
}

.seg-item {
  flex: 1 1 28%;
  min-width: 160rpx;
  min-height: var(--tap-md);
  padding: var(--space-sm) var(--space-md);
  box-sizing: border-box;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  background: var(--color-fill-ghost);
  border: 2rpx solid transparent;
  border-radius: 14rpx;
  color: var(--color-neutral-500);
  font-size: var(--fs-body);
  white-space: normal;
  line-height: 1.3;
}

.seg-item.on {
  background: var(--color-bg-card);
  border-color: var(--color-border-brand);
  color: var(--color-text-primary);
  font-weight: 600;
}
